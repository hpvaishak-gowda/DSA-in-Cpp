class Solution {
	public:
	vector<int> printList(Node *head) {
		vector<int> ans;
		
		Node* temp=head;
		
		while(temp != NULL){
		    ans.push_back(temp->data);
		    temp=temp->next;
		}
		
		return ans;
	}
};
